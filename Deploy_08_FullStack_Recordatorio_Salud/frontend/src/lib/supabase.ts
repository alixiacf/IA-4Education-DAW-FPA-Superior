// Este archivo se elimina ya que no utilizamos Supabase
// Se mantiene únicamente para evitar errores de importación mientras se completa la migración

// No importamos la base de datos de Supabase
// import { Database } from './database.types';

console.log('Usando MongoDB directamente a través de la API');

// Este objeto debería eliminarse completamente en el futuro
export const supabase = null;